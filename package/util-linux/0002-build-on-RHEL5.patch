diff -burp host-util-linux-2.30.1/libmount/src/monitor.c host-util-linux-2.30.1-mod/libmount/src/monitor.c
--- host-util-linux-2.30.1/libmount/src/monitor.c	2017-06-14 12:35:49.914058000 -0700
+++ host-util-linux-2.30.1-mod/libmount/src/monitor.c	2017-10-24 11:15:46.112181000 -0700
@@ -282,7 +282,7 @@ static int userspace_monitor_get_fd(stru
 	assert(me->path);
 	DBG(MONITOR, ul_debugobj(mn, " open userspace monitor for %s", me->path));
 
-	me->fd = inotify_init1(IN_NONBLOCK | IN_CLOEXEC);
+	me->fd = -1;//inotify_init1(IN_NONBLOCK | IN_CLOEXEC);
 	if (me->fd < 0)
 		goto err;
 
@@ -652,7 +652,7 @@ int mnt_monitor_get_fd(struct libmnt_mon
 		return mn->fd;
 
 	DBG(MONITOR, ul_debugobj(mn, "create top-level monitor fd"));
-	mn->fd = epoll_create1(EPOLL_CLOEXEC);
+	mn->fd = -1;//epoll_create1(EPOLL_CLOEXEC);
 	if (mn->fd < 0)
 		return -errno;
 
diff -burp host-util-linux-2.30.1/libmount/src/utils.c host-util-linux-2.30.1-mod/libmount/src/utils.c
--- host-util-linux-2.30.1/libmount/src/utils.c	2017-06-19 02:58:19.259003000 -0700
+++ host-util-linux-2.30.1-mod/libmount/src/utils.c	2017-10-24 11:23:13.028507000 -0700
@@ -848,7 +848,7 @@ int mnt_open_uniq_filename(const char *f
 	 */
 	oldmode = umask(S_IRGRP|S_IWGRP|S_IXGRP|
 			S_IROTH|S_IWOTH|S_IXOTH);
-	fd = mkostemp(n, O_RDWR|O_CREAT|O_EXCL|O_CLOEXEC);
+	fd = mkstemp(n);
 	if (fd < 0)
 		fd = -errno;
 	umask(oldmode);
